// The file contains the locators for the Profile page.


if(maestro.platform === "ios") {
    output.profileButton = "Profile, tab, 5 of 5"
};
if(maestro.platform === "android") {
    output.profileButton = "Profile"
};

output.Profile = {
    messageLogout: "Are you sure you want to logout?",
    settingsProfile: "SettingsProfile",
    logoutButton: "Logout",
    confirmLogoutButton: "Confirm"
};          